const A=document.getElementById('app');let map,markers={},currentTab='command';let sos=[];const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
function layout(){A.innerHTML=`<div class="shell"><aside class="side"><div class="brand">Rescu<span>EZ</span></div><div class="tag">EMERGENCY RESPONSE NETWORK</div><div class="nav"><button class="active" onclick="tab('command',this)">⌁ Command Center</button><button onclick="tab('victim',this)">🚨 Report SOS</button><button onclick="tab('incidents',this)">◉ Incidents</button><button onclick="tab('resources',this)">▣ Resources</button><button onclick="tab('algorithms',this)">⌁ Routing Intelligence</button></div><div class="notice" style="margin-top:25px">DEMO / SIMULATION<br><span class="muted">Synthetic data only. Not connected to emergency authorities.</span></div></aside><main class="main" id="view"></main></div>`;renderCommand()}
function tab(t,el){currentTab=t;document.querySelectorAll('.nav button').forEach(x=>x.classList.remove('active'));el?.classList.add('active');({command:renderCommand,victim:renderVictim,incidents:renderIncidents,resources:renderResources,algorithms:renderAlgorithms}[t]||renderCommand)()}
async function load(){try{const r=await fetch('/api/sos');sos=(await r.json()).sos||[]}catch(e){sos=[]}renderCommand()}
function renderCommand(){const v=document.getElementById('view');if(!v)return;const crit=sos.filter(x=>x.priority==='CRITICAL'&&x.status!=='RESOLVED').length,active=sos.filter(x=>x.status!=='RESOLVED').length,people=sos.reduce((a,x)=>a+(+x.people||0),0);v.innerHTML=`<div class="top"><div><div class="muted">EMERGENCY OPERATIONS CENTER</div><h2 style="margin:3px 0">Live Response Network</h2></div><div class="row"><span class="status">● SYSTEM OPERATIONAL</span><button class="danger" onclick="openSOS()">SEND SOS</button></div></div><div class="hero"><h1>One SOS. The right help. The fastest route.</h1><p>Real-time crowd-sourced emergency reporting, priority intelligence, resource matching and route optimization.</p></div><div class="cards"><div class="card kpi"><small>ACTIVE SOS</small><b>${active}</b></div><div class="card kpi"><small>CRITICAL</small><b>${crit}</b></div><div class="card kpi"><small>PEOPLE AFFECTED</small><b>${people}</b></div><div class="card kpi"><small>RESOLVED</small><b>${sos.filter(x=>x.status==='RESOLVED').length}</b></div></div><div class="grid"><div class="card"><div class="row space"><h3>Live Incident Map</h3><span class="muted">Exact location visible to authorized command staff</span></div><div id="map" class="map"></div></div><div class="card"><div class="row space"><h3>Priority Queue</h3><span class="muted">${sos.length} total</span></div><div class="queue">${sos.length?sos.map(card).join(''):`<div class="notice">No active SOS requests.</div>`}</div></div></div>`;setTimeout(initMap,20)}
function card(x){return `<div class="sos ${x.priority==='CRITICAL'?'critical':''}"><div class="row space"><span class="pill ${x.priority}">${esc(x.priority)}</span><b>${esc(x.id)}</b></div><h3 style="margin:9px 0 4px">${esc(x.category)}</h3><div class="muted">${esc(x.name)} · ${+x.people||1} people · ${esc(x.status)}</div><div class="muted" style="margin-top:5px">${esc(x.needs||'Emergency assistance')} · Priority ${x.priorityScore}/100</div><div class="row" style="margin-top:10px"><button class="ghost" onclick="detail('${x.id}')">View details</button><button class="primary" onclick="dispatch('${x.id}')">Dispatch help</button></div></div>`}
function initMap(){if(!document.getElementById('map'))return;if(map)map.remove();map=L.map('map').setView([15.8281,78.0373],13);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap'}).addTo(map);sos.forEach(x=>{const c=x.priority==='CRITICAL'?'#ef476f':x.priority==='HIGH'?'#ffb347':'#36d7ff';const m=L.circleMarker([x.lat,x.lng],{radius:9,color:c,fillColor:c,fillOpacity:.85,weight:2}).addTo(map).bindPopup(`<b>${esc(x.id)}</b><br>${esc(x.category)}<br>${esc(x.name)}<br>Priority ${x.priorityScore}/100`);markers[x.id]=m})}
function openSOS(){renderVictim()}
function renderVictim(){document.getElementById('view').innerHTML=`<div class="top"><div><div class="muted">CITIZEN / VICTIM PORTAL</div><h2 style="margin:3px 0">Request Emergency Assistance</h2></div><button class="ghost" onclick="tab('command',document.querySelector('.nav button'))">Back to command center</button></div><div class="card" style="max-width:850px"><div class="notice">Your location is required so authorized responders can find you. Browser GPS will be used when available. You can also enter coordinates manually.</div><div class="formgrid"><div class="field"><label>Full name *</label><input id="name" placeholder="Your name"></div><div class="field"><label>Phone number</label><input id="phone" placeholder="+91 ..."></div><div class="field"><label>Emergency type *</label><select id="category"><option>Medical</option><option>Flood</option><option>Trapped Person</option><option>Rescue</option><option>Food</option><option>Drinking Water</option><option>Shelter</option><option>Fire</option><option>Other</option></select></div><div class="field"><label>People affected</label><input id="people" type="number" min="1" value="1"></div><div class="field"><label>Severity</label><select id="severity"><option value="25">Low</option><option value="50">Medium</option><option value="75" selected>High</option><option value="90">Critical</option></select></div><div class="field"><label>Immediate needs</label><input id="needs" placeholder="Water, first aid, rescue..."></div><div class="field"><label>Latitude *</label><input id="lat" placeholder="15.8281"></div><div class="field"><label>Longitude *</label><input id="lng" placeholder="78.0373"></div><div class="field full"><label>Description</label><textarea id="desc" placeholder="Describe what is happening..."></textarea></div></div><div class="row" style="margin-top:14px"><button class="ghost" onclick="gps()">◎ Use my current location</button><button class="danger" onclick="submitSOS()">🚨 CONFIRM & SEND SOS</button></div><div id="gpsmsg" class="muted" style="margin-top:10px"></div></div>`}
function gps(){if(!navigator.geolocation)return document.getElementById('gpsmsg').textContent='Geolocation unavailable. Enter coordinates manually.';document.getElementById('gpsmsg').textContent='Detecting location…';navigator.geolocation.getCurrentPosition(p=>{lat.value=p.coords.latitude.toFixed(6);lng.value=p.coords.longitude.toFixed(6);document.getElementById('gpsmsg').textContent='Location detected successfully.'},()=>document.getElementById('gpsmsg').textContent='Location permission unavailable. Enter coordinates manually.')}
async function submitSOS(){const b={name:name.value.trim(),phone:phone.value.trim(),category:category.value,people:+people.value||1,severityScore:+severity.value,needs:needs.value.trim(),description:desc.value.trim(),lat:+lat.value,lng:+lng.value,medical:category.value==='Medical',trapped:category.value==='Trapped Person'};if(!b.name||!Number.isFinite(b.lat)||!Number.isFinite(b.lng)){alert('Please provide your name and valid location coordinates.');return}const r=await fetch('/api/sos',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});const d=await r.json();if(!r.ok)return alert(d.error||'Unable to send SOS');sos.unshift(d.sos);document.getElementById('view').innerHTML=`<div class="card" style="max-width:700px;margin:60px auto;text-align:center"><div style="font-size:55px">✓</div><h1>SOS SENT</h1><p class="muted">Your request has been securely recorded for authorized command staff.</p><div class="detail"><div><b>REFERENCE</b>${esc(d.sos.id)}</div><div><b>PRIORITY</b>${esc(d.sos.priority)} · ${d.sos.priorityScore}/100</div><div><b>LOCATION</b>${d.sos.lat}, ${d.sos.lng}</div><div><b>STATUS</b>NEW — Awaiting assignment</div></div><button class="primary" style="margin-top:16px" onclick="tab('command',document.querySelector('.nav button'))">Open Command Center</button></div>`}
function detail(id){const x=sos.find(a=>a.id===id);if(!x)return;const v=document.createElement('div');v.className='modal';v.innerHTML=`<div class="modalbox"><div class="row space"><h2>${esc(x.id)}</h2><button class="ghost" onclick="this.closest('.modal').remove()">Close</button></div><span class="pill ${x.priority}">${esc(x.priority)} · ${x.priorityScore}/100</span><div class="detail section"><div><b>Victim</b>${esc(x.name)}</div><div><b>Phone</b>${esc(x.phone||'Not provided')}</div><div><b>Emergency</b>${esc(x.category)}</div><div><b>People</b>${x.people}</div><div><b>Needs</b>${esc(x.needs||'Not specified')}</div><div><b>Status</b>${esc(x.status)}</div><div><b>Latitude</b>${x.lat}</div><div><b>Longitude</b>${x.lng}</div></div><div class="notice">Priority engine: severity + people affected + medical/trapped modifiers. Exact coordinates are available to this authorized command-center view.</div><div class="row"><button class="primary" onclick="dispatch('${x.id}');this.closest('.modal').remove()">Assign & Dispatch</button><button class="ghost" onclick="window.open('https://www.google.com/maps?q=${x.lat},${x.lng}','_blank')">Open location</button></div></div>`;document.body.appendChild(v)}
async function dispatch(id){const r=await fetch('/api/sos/'+id,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'ASSIGNED',assignedAt:new Date().toISOString()})});if(r.ok){const d=await r.json();const i=sos.findIndex(x=>x.id===id);sos[i]=d.sos;renderCommand();alert('Responder assignment created. Route optimization can now be initiated for '+id+'.')}}
function renderIncidents(){document.getElementById('view').innerHTML=`<div class="top"><h2>Incident Registry</h2><span class="muted">Database-backed SOS records</span></div><div class="card">${sos.length?sos.map(x=>`<div class="row space" style="padding:14px 0;border-bottom:1px solid #173047"><div><b>${esc(x.id)}</b> · ${esc(x.category)}<div class="muted">${esc(x.name)} · ${esc(x.createdAt)}</div></div><span class="pill ${x.priority}">${esc(x.priority)}</span><button class="ghost" onclick="detail('${x.id}')">View</button></div>`).join(''):'No incidents yet.'}</div>`}
function renderResources(){document.getElementById('view').innerHTML=`<div class="top"><h2>Resource Readiness</h2></div><div class="cards"><div class="card kpi"><small>WATER</small><b>1,240</b></div><div class="card kpi"><small>FIRST AID</small><b>184</b></div><div class="card kpi"><small>FOOD PACKETS</small><b>760</b></div><div class="card kpi"><small>BLANKETS</small><b>420</b></div></div><div class="card"><h3>Intelligent allocation</h3><p class="muted">The production architecture reserves inventory transactionally, compares supplier availability, travel time, route safety and workload, then recommends a load-balanced allocation.</p></div>`}
function renderAlgorithms(){document.getElementById('view').innerHTML=`<div class="top"><h2>Routing Intelligence</h2></div><div class="grid"><div class="card"><h3>A* Pathfinding</h3><p class="muted">Uses a geographic heuristic to efficiently find a low-cost route while accounting for blocked and high-risk road segments.</p><div class="notice">Cost = distance + travel time + risk + blockage penalty</div></div><div class="card"><h3>Dijkstra</h3><p class="muted">Calculates shortest path without a heuristic. Useful as a transparent baseline for algorithm comparison.</p><div class="notice">Both algorithms are designed to run on a local graph fallback when a map-routing API is unavailable.</div></div></div><div class="card section"><h3>Why RescuEZ?</h3><p>It does not blindly choose the nearest supplier. The intended matching engine combines resource availability, travel time, severity, supplier capacity, route safety and load balancing.</p></div>`}
layout();load();setInterval(async()=>{try{const r=await fetch('/api/sos');const n=(await r.json()).sos||[];if(JSON.stringify(n)!==JSON.stringify(sos)){sos=n;if(currentTab==='command')renderCommand()}}catch{}},3000);
